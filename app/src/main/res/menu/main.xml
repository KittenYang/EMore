<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/mainMenus" android:orderInCategory="100">
    <item
        android:id="@+id/publish" android:icon="@mipmap/ic_menu_edit_white"
        android:orderInCategory="2"
        android:title="@string/publish" app:showAsAction="always"/>
    <item
        android:id="@+id/search" android:icon="@mipmap/ic_search_white_24dp"
        android:orderInCategory="2"
        android:title="@string/main_search" app:showAsAction="always"/>
    </group>

</menu>